# Expo + pnpm monorepo: a flat node_modules so Metro resolves React Native + Expo
# deps deterministically (the Expo "work with monorepos" guidance for pnpm). The
# backend packages are dep-free / Deno, so hoisting has no effect on them.
node-linker=hoisted
