// The character's workshop. Local inspection only — not linked from the product.

import { BloubLab } from "@/components/dev/bloub-lab/lab";

export const metadata = { title: "Nemesis character lab" };

export default function BloubLabPage() {
  return <BloubLab />;
}
