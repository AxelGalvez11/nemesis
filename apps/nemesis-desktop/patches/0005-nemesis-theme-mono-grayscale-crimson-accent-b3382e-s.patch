From 446f3183d93aaeb2ca4f695d5da85f632a34980c Mon Sep 17 00:00:00 2001
From: AxelGalvez11 <axelgalvez1121@gmail.com>
Date: Thu, 9 Jul 2026 19:40:22 -0500
Subject: [PATCH 05/14] =?UTF-8?q?nemesis:=20theme=20=E2=80=94=20mono=20gra?=
 =?UTF-8?q?yscale=20+=20crimson=20accent=20(#b3382e),=20set=20as=20default?=
 =?UTF-8?q?=20skin?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

---
 apps/desktop/src/themes/presets.js | 36 ++++++++++++++++++++++++++++-
 apps/desktop/src/themes/presets.ts | 37 +++++++++++++++++++++++++++++-
 2 files changed, 71 insertions(+), 2 deletions(-)

diff --git a/apps/desktop/src/themes/presets.js b/apps/desktop/src/themes/presets.js
index f61ec2c..7d52355 100644
--- a/apps/desktop/src/themes/presets.js
+++ b/apps/desktop/src/themes/presets.js
@@ -189,6 +189,39 @@ export const monoTheme = {
         userBubbleBorder: '#363636'
     }
 };
+/** The Nemesis look: mono's grayscale field with a deep-crimson accent. Deliberately
+ *  distinct from the danger red (`--ui-red` #cf2d56) so "brand" never reads as "delete". */
+export const nemesisTheme = {
+    name: 'nemesis',
+    label: 'Nemesis',
+    description: 'Monochrome with a red edge',
+    colors: {
+        background: '#0e0e0e',
+        foreground: '#eaeaea',
+        card: '#141414',
+        cardForeground: '#eaeaea',
+        muted: '#1e1e1e',
+        mutedForeground: '#808080',
+        popover: '#181818',
+        popoverForeground: '#eaeaea',
+        primary: '#b3382e',
+        primaryForeground: '#fff5f3',
+        secondary: '#262626',
+        secondaryForeground: '#c8c8c8',
+        accent: '#261a18',
+        accentForeground: '#e8c7c2',
+        border: '#2a2a2a',
+        input: '#2a2a2a',
+        ring: '#c04a40',
+        midground: '#b3382e',
+        destructive: '#a84040',
+        destructiveForeground: '#fef2f2',
+        sidebarBackground: '#0a0a0a',
+        sidebarBorder: '#202020',
+        userBubble: '#1f1717',
+        userBubbleBorder: '#3d2b28'
+    }
+};
 /** Neon green on black. Matches the CLI cyberpunk skin and dashboard theme. */
 export const cyberpunkTheme = {
     name: 'cyberpunk',
@@ -261,6 +294,7 @@ export const slateTheme = {
     }
 };
 export const BUILTIN_THEMES = {
+    nemesis: nemesisTheme,
     nous: nousTheme,
     midnight: midnightTheme,
     ember: emberTheme,
@@ -270,4 +304,4 @@ export const BUILTIN_THEMES = {
 };
 export const BUILTIN_THEME_LIST = Object.values(BUILTIN_THEMES);
 /** Skin used when nothing is persisted or the persisted name is retired. */
-export const DEFAULT_SKIN_NAME = 'mono';
+export const DEFAULT_SKIN_NAME = 'nemesis';
diff --git a/apps/desktop/src/themes/presets.ts b/apps/desktop/src/themes/presets.ts
index 636df22..1ddde4d 100644
--- a/apps/desktop/src/themes/presets.ts
+++ b/apps/desktop/src/themes/presets.ts
@@ -204,6 +204,40 @@ export const monoTheme: DesktopTheme = {
   }
 }
 
+/** The Nemesis look: mono's grayscale field with a deep-crimson accent. Deliberately
+ *  distinct from the danger red (`--ui-red` #cf2d56) so "brand" never reads as "delete". */
+export const nemesisTheme: DesktopTheme = {
+  name: 'nemesis',
+  label: 'Nemesis',
+  description: 'Monochrome with a red edge',
+  colors: {
+    background: '#0e0e0e',
+    foreground: '#eaeaea',
+    card: '#141414',
+    cardForeground: '#eaeaea',
+    muted: '#1e1e1e',
+    mutedForeground: '#808080',
+    popover: '#181818',
+    popoverForeground: '#eaeaea',
+    primary: '#b3382e',
+    primaryForeground: '#fff5f3',
+    secondary: '#262626',
+    secondaryForeground: '#c8c8c8',
+    accent: '#261a18',
+    accentForeground: '#e8c7c2',
+    border: '#2a2a2a',
+    input: '#2a2a2a',
+    ring: '#c04a40',
+    midground: '#b3382e',
+    destructive: '#a84040',
+    destructiveForeground: '#fef2f2',
+    sidebarBackground: '#0a0a0a',
+    sidebarBorder: '#202020',
+    userBubble: '#1f1717',
+    userBubbleBorder: '#3d2b28'
+  }
+}
+
 /** Neon green on black. Matches the CLI cyberpunk skin and dashboard theme. */
 export const cyberpunkTheme: DesktopTheme = {
   name: 'cyberpunk',
@@ -278,6 +312,7 @@ export const slateTheme: DesktopTheme = {
 }
 
 export const BUILTIN_THEMES: Record<string, DesktopTheme> = {
+  nemesis: nemesisTheme,
   nous: nousTheme,
   midnight: midnightTheme,
   ember: emberTheme,
@@ -289,4 +324,4 @@ export const BUILTIN_THEMES: Record<string, DesktopTheme> = {
 export const BUILTIN_THEME_LIST = Object.values(BUILTIN_THEMES)
 
 /** Skin used when nothing is persisted or the persisted name is retired. */
-export const DEFAULT_SKIN_NAME = 'mono'
+export const DEFAULT_SKIN_NAME = 'nemesis'
-- 
2.50.1 (Apple Git-155)

