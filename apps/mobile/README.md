# @pharmabro/mobile

React Native + Expo app — **initialized in Phase 6**, built against the **frozen §8 API
contract** (locked at the end of Phase 3). See `IMPLEMENTATION_PLAN.md` §12 for the
screen list, 4-tab nav (Ask · Explore · Watchlist · Profile), and the 8-state matrix.

This is an intentional placeholder so the pnpm workspace resolves during the
backend-first phases (0–5).
