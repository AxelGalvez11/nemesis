# champ: observed component inventory

See `DESIGN_ANALYSIS.md` in this directory for measured values and the reasoning.



Marketing only; no application surface is reachable. The thinnest sample in the set.

| component | measured |
| --- | --- |
| **Button (primary)** | 40px, `8px 22px`, pill, `14/600`, bg `#1a1a1a` |
| **Button (accent)** | 40px, `0 14px`, pill, `16/700`, bg `#da7007` |
| **Nav link** | 40px, `12px 0`, radius 0, `16/400`, opacity transition only |
| **Display heading** | `32/600` with `-0.8px` |
| **Body** | `16/400/19.2px` |

Only two component types are meaningfully distinct. champ contributes the warm ground and the
single-accent discipline, and nothing structural.
